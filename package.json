{
  "name": "legitsl-web-core",
  "private": true,
  "version": "0.1.5",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "prepare": "tsc && vite build --base /lsl-editor/legitsl/"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "terser": "^5.36.0"
  },
  "files": [
    "dist"
  ]
}
